<Window x:Class="AppForTourment.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppForTourment"
        mc:Ignorable="d"
        Title="LoginWindow" Height="720" Width="1280">
    <Grid>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Height="500">
            <Border BorderBrush="LightGray" BorderThickness="2" CornerRadius="30">
                <ListView x:Name="ll" BorderBrush="Black" BorderThickness="10">
                    <!--<ListViewItem Height="70" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBox Text="Вход" Foreground="Black" FontSize="22" BorderThickness="0"/>
                    </ListViewItem>
                    <ListViewItem Height="70">
                        <PasswordBox HorizontalAlignment="Center" VerticalAlignment="Center" Margin="3 0 3 0" PasswordChanged="passwordBox_PasswordChanged" BorderBrush="LightGray" BorderThickness="0.5">
                        </PasswordBox>
                    </ListViewItem>-->
                </ListView>
            </Border>
        </Grid>
    </Grid>
</Window>
